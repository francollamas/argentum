name: continuous integration

on:
    pull_request:
    push:
        branches: ['develop', 'main']

jobs:
    continuous-integration:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v4

            - uses: pnpm/action-setup@v4
              with:
                  version: 10.18.0

            - name: setup node
              uses: actions/setup-node@v4
              with:
                  node-version: lts/*

            - name: Install Free Tex Packer
              run: pnpm add -g free-tex-packer-cli

            - name: Install dependencies
              run: pnpm i

            - name: Generate packed textures
              run: pnpm run generate-assets

            - name: Build
              run: pnpm run build

            - name: Test
              run: pnpm test

            - name: Linter and Format
              run: npm run linter-check
